# Defaults for beanstalkd initscript
# sourced by /etc/init.d/beanstalkd

# set enabled to 1 if you want the init script to start beanstalkd 
ENABLED=1

# listen on address
BEANSTALKD_ADDR=0.0.0.0

# listen on port
BEANSTALKD_PORT=11300

# beanstalkd user
BEANSTALKD_USER=beanstalkd
