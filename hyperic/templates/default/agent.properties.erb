agent.setup.camIP=<%= @server %>
agent.setup.camPort=*default*
agent.setup.camSSLPort=443
agent.setup.camSecure=yes
agent.setup.camLogin=<%= @login %>
agent.setup.camPword=<%= @password %>
agent.setup.agentIP=<%= @ip %>
agent.setup.agentPort=*default*
agent.setup.unidirectional=no