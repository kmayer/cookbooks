set daemon 30
set logfile syslog facility log_daemon 
set mailserver localhost
set alert darrin@heavywater.ca only on { timeout }

set httpd port 2812
  and use the address localhost
  allow localhost